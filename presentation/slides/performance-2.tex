\begin{frame}
\frametitle{Performance: Another Look}
\includegraphics[width=\textwidth]{res/generalizeability-performance.png}
\end{frame}

% I repeated the sqlmap dataset creation process with a second API endpoint that had a SQL injection vulnerability, but that didn't require an apostrophe to exploit.
% In this case, the input was a number instead of a string, so no single quote was required to terminate the string delimiter for valid SQL syntax.